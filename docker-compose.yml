version: "3.8"

services:
  agent:
    build: .
    container_name: instagram-repost-agent
    restart: always
    env_file:
      - .env
    volumes:
      - ./data:/app/data
      - ./logs:/app/logs
      - ./downloads:/app/downloads
